# Backend Django (Legacy)

Questo backend Django REST non è più utilizzato in produzione.

L'applicazione è stata migrata a **Next.js + Supabase** per il deploy su Vercel.

Vedi il [README principale](../README.md) per il setup attuale.
